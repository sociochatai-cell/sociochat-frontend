export default function AgentsManager(props: Record<string, any>) {
    return (
        <div className="p-6 text-center text-muted-foreground">
            <p className="text-sm">AI Agents management will be available in a future update.</p>
        </div>
    );
}
